1. Issue LCU PayMe Token

  [TestMethod]
 public void UseCase_coin_payme()
 {
     // PayToSin combined Mint Coin plus Process Coin

     Set_AUD();
     string folder = Environment.GetEnvironmentVariable("Envionment");

     // Credit
     // Generate PayMeToken
     string credit_sin = Environment.GetEnvironmentVariable("SinB");
     string userTokenPath = @"D:\Examples\" + node + @"\" + folder + "\\Token-" + credit_sin + ".json";

     Fido.FidoProvider fp = new Fido.FidoProvider(userTokenPath);
     string userToken = fp.GetJwToken();
     decimal amount = 8; // AUD

      Uri _baseUri = new Uri(fp.Audience());

     // Obtain JwToken
     string samount = amount.ToString("N2");
     HttpClient _httpClient = new HttpClient();
     string _relativeUrl = "Coin/IssuePayMeToken?amount=" + samount;
     Uri _uri = new Uri(_baseUri, _relativeUrl);
     _httpClient.BaseAddress = _baseUri;
     // Add Device Jwtoken
     _httpClient.DefaultRequestHeaders.Add("x-token", fp.GetJwToken());
     // Add device signature
     string shash = fp.GetSecureIdentity() + samount;
     string signature = fp.Sign(shash);
     _httpClient.DefaultRequestHeaders.Add("x-jws-signature", signature);

     // Add Device Jwtoken
     _httpClient.DefaultRequestHeaders.Add("x-counter", fp.GetCounter().ToString());
     // Get response
     HttpResponseMessage response = _httpClient.GetAsync(_uri).Result;
     string payMeToken = string.Empty;
     if (response.IsSuccessStatusCode)
     {
         payMeToken = response.Content.ReadAsStringAsync().Result;
     }
     else
     {
         string sError = response.Content.ReadAsStringAsync().Result;
         throw new Exception(sError);
     }
 }
